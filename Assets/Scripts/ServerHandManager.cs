using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This class exists to keep opponent's hand data invisible on a client
// might work on this in the future where I actually need to start caring about security
public class ServerHandManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
